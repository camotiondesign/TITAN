# Row 12 — Tariq quote single — VOICES

> **Editorial call (Cam via Claude, 28 Aug 2026):** "Be more dentist" strapline has zero pickup in the sector 12 months on (per `tariq-ps25-claims-audit.md`). Bury it. Pick a different line from the PS25 transcript — the "we didn't focus on our image" line, or the "own dispensing 100% control 0% involvement" line, both carry more substance.

**Target publish:** Wk 4 — Week 4 — Lightweight
**Reason to care:** VALUE — one line carries the post, caption reads into the quote
**Format:** LI single-image (VOICES grammar) + IG + FB
**Cross-platform:** As specified by the row itself — LI-Titan PMR + Instagram + Facebook (Titanverse cross-post only if the chosen line touches both brands).

## The idea in one paragraph

One standalone Tariq line, presented plain with attribution — no argument-building, no carousel scaffolding. Candidates per the v5.1 roster include "Instinct built without data is just hope." The caption's job is to read into the quote, not restate it.

## Source material (with exact paths where they exist)

**Path convention:** same dual-root pattern as Row 1 — LaCie canonical, currently empty; GDrive current working copy. See Row 1's brief for the full roots.

- `Tariqs Talk - v1_1_otter_ai.txt` (+ `.mp3` for audio cross-check, folder root) — PS25 source. Available now on GDrive; not yet on LaCie.
- TitanUp 26 keynote transcript (`_context/titanup26-keynote-transcript.md` per v5.1's reading order — path unconfirmed in this repo, same caveat as Row 11) — for a TitanUp-sourced alternative line.
- v5.1 §6.6 (names "Instinct built without data is just hope" and "Titan means business" as exemplar lines in the quote series), roster #8.

**TO VERIFY flag from v5.1 §9.1:** the "Campaign visual language" P0 row lists "Quote inventory audit against verified sources" as outstanding — the master Tariq quote inventory this row is meant to draw from hasn't been formally audited yet. A single quote can be manually verified without waiting for the full inventory, but note that bench-07 (additional Tariq VOICES singles) does depend on that fuller audit.

## Production steps

1. Select one standalone-worthy Tariq line.
2. Verify it verbatim against source audio, not just the Otter transcript (Otter is auto-generated and can mis-transcribe).
3. Design against VOICES grammar (portraits, quotes, attribution).
4. Caption reads into the quote — doesn't restate it.

## Ownership / status

- Owner: TBD.
- Blockers: none material for a single manually-verified quote; the fuller inventory audit remains open for bench-07.

## Success criteria

`single-image` cohort. Lightweight job: a personality/rhythm beat — one strong line carrying its own weight.

## Voice notes

**Broadcaster** — "present the quote plain, with attribution, let it work" (v5.1 roster #8). Hook territory: **Quote**.

## Wahid direction check (if applicable)

Only relevant if the chosen line is "Titan means business" itself — if so, this would foreshadow Row 20's argument. Recommend picking a different line (e.g. "Instinct built without data is just hope") here and saving "Titan means business" for Row 20, so the phrase isn't spent early.
